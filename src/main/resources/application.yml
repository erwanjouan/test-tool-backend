server:
  servlet:
    context-path: /tnr
spring:
  application:
    name: backend
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect

management:
  endpoint:
    health:
      probes:
        enabled: true